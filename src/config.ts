/**
 * When true, Quick Clean uses a small corner test mask instead of automatic overlay detection.
 * Keep false in production so real overlays are processed (or the user is asked to use Manual Restore).
 */
export const TEST_PROCESSING_PIPELINE = false;

export const PRODUCT_NAME = "EraseMark";
export const LOG_PREFIX = "[EraseMark]";
export const LOG_ERROR_PREFIX = "[EraseMark ERROR]";
